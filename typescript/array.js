//  here we will talk more sabout arrays and how to use them in typescript 
var superHeroes = []; // this is one way of declaring a specific typer of array in ts 
var superHeroes2 = []; // this is another way of declaring a specific type of array in ts
var userArray = []; // here we are creating an array of objects with the type User
userArray.push({ name: "John", email: "bhav" }); // here we are pushing an object of type User in the array
console.log(userArray);
